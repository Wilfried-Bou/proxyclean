---
import Layout from "../layouts/Layout.astro";
import Breadcrumb from "../components/Breadcrumb.astro";
---
<Layout title="Rejoindre Proxi'Clean | Envoyez votre CV"
        description="Vous voulez rejoindre l'entreprise Proxi'Clean services ? N'hésitez pas à nous faire parvenir votre CV.">

    <Breadcrumb currentPage="Rejoignez-nous"></Breadcrumb>

    <section class="py-12 bg-theme text-white text-center">
        <h1 class="text-4xl font-bold uppercase tracking-widest text-white">Rejoignez Proxi'Clean services</h1>
        <p class="mt-2 text-lg text-white">Envoyez nous votre CV en quelques clics</p>
    </section>

    <section class="py-24 bg-gray-50 px-6">
        <div class="max-w-6xl mx-auto bg-white rounded-[2rem] shadow-2xl overflow-hidden flex flex-col md:flex-row min-h-[600px]">
            
            <div class="bg-primaryTheme p-12 md:p-16 text-white md:w-2/5 flex flex-col justify-center">
                <h2 class="text-3xl font-bold mb-6">Travailler chez nous</h2>
                <p class=" mb-6 font-medium">Nous sommes toujours à la recherche de nouveaux talents rigoureux et motivés.</p>
                <ul class="space-y-4 text-sm">
                    <li class="flex items-center gap-2">✅ CDI / CDD / Temps partiel</li>
                    <li class="flex items-center gap-2">✅ Équipement de qualité</li>
                </ul>
            </div>

            <form 
                id="recrutement-form"
                action="https://forminit.com/f/uo9rkoz3db3" 
                method="POST" 
                enctype="multipart/form-data"
                class="p-10 flex-1 grid grid-cols-1 gap-5"
            >
                <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                    <div class="flex flex-col gap-1.5">
                        <label for="name" class="text-sm font-semibold text-gray-700 ml-1">Nom Complet</label>
                        <input id="name" type="text" name="fi-sender-fullName" required class="input input-bordered focus:outline-secondaryTheme" />
                    </div>
                    <div class="flex flex-col gap-1.5">
                        <label for="phone" class="text-sm font-semibold text-gray-700 ml-1">Téléphone</label>
                        <input id="phone" type="tel" name="fi-text-phone" required class="input input-bordered focus:outline-secondaryTheme" />
                    </div>
                </div>

                <div class="flex flex-col gap-1.5">
                    <label for="email" class="text-sm font-semibold text-gray-700 ml-1">Email</label>
                    <input id="email" type="email" name="fi-sender-email" required class="input input-bordered focus:outline-secondaryTheme" />
                </div>

                <div class="flex flex-col gap-1.5">
                    <label for="cv-file" class="text-sm font-semibold text-gray-700 ml-1">Votre CV (PDF uniquement)</label>
                    <input 
                        id="cv-file"
                        type="file" 
                        name="fi-file-cv" 
                        accept=".pdf" 
                        required 
                        class="file-input file-input-bordered file-input-primary w-full focus:outline-secondaryTheme"
                    />
                    <p class="text-[10px] text-gray-400 italic mt-1">Maximum 5Mo. Format PDF uniquement.</p>
                </div>

                <div class="flex flex-col gap-1.5">
                    <label for="message" class="text-sm font-semibold text-gray-700 ml-1">Message (Optionnel)</label>
                    <textarea id="message" name="fi-text-message" class="textarea textarea-bordered h-24 focus:outline-secondaryTheme"></textarea>
                </div>

                <div class="flex items-start gap-2 py-2">
                    <input type="checkbox" required class="checkbox checkbox-sm checkbox-primary mt-1" />
                    <span class="text-[10px] text-gray-500 leading-tight">J'accepte que mes données soient conservées...</span>
                </div>

                <button 
                    id="submit-btn" 
                    type="submit" 
                    class="btn btn-lg bg-primaryTheme text-white border-none hover:bg-secondaryTheme transition-colors mt-4">
                    Envoyer ma candidature
                </button>
            </form>

            <div 
                id="toast" 
                class="hidden fixed bottom-5 right-5 bg-green-600 text-white px-8 py-5 rounded-2xl shadow-2xl z-50 transition-all duration-300 transform translate-y-10">
                ✅ Candidature envoyée avec succès !
            </div>
        </div>
    </section>

    <script is:inline>
        // 1. TA CLÉ FORMINIT
        const FORM_ID = 'uo9rkoz3db3'; 
        
        const form = document.getElementById('recrutement-form');
        const toast = document.getElementById('toast');
        const btn = document.getElementById('submit-btn');

        // 2. CHARGEMENT DU SDK FORMINIT (indispensable pour Astro)
        const script = document.createElement('script');
        script.src = 'https://forminit.com/sdk/v1/forminit.js';
        script.onload = () => {
            const forminit = new Forminit();

            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                btn.disabled = true;
                btn.innerText = "Envoi en cours...";

                const formData = new FormData(form);

                // 3. UTILISATION DE LA MÉTHODE SDK
                const { data, error } = await forminit.submit(FORM_ID, formData);

                if (error) {
                    alert("Erreur : " + error.message);
                    btn.disabled = false;
                    btn.innerText = "Envoyer ma candidature";
                } else {
                    // SUCCÈS
                    toast.classList.remove('hidden', 'translate-y-10');
                    form.reset();
                    btn.disabled = false;
                    btn.innerText = "Envoyer ma candidature";
                    setTimeout(() => {
                        toast.classList.add('hidden', 'translate-y-10');
                    }, 5000);
                }
            });
        };
        document.body.appendChild(script);
    </script>
</Layout>